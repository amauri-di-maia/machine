version: 1

paths:
  repo_root: "."
  sqlite_db: "data base/brickovery.db"
  lockfile: "data base/locks/brickovery.lock"
  manifests_dir: "data base/manifests"
  logs_dir: "data base/logs"

tables:
  shipping_normal_txt: "data/tables/shipping_normal.txt"
  shipping_registered_txt: "data/tables/shipping_registered.txt"
  rarity_rules_txt: "data/tables/rarity_rules.txt"

upstream:
  repo: "amauri-di-maia/amauri-repo"
  ref: "729a81bc66ed40d77807588a9fb15b912ef93e8710c2f676f28752f20fbf695e"
  db_relpath: "data base/brickovery.db"
  schema_hint:
    mapping_table: "brickovery_db"
    bl_part_col: "bl_part_id"
    bl_color_col: "bl_color_id"
    bl_itemtype_col: "item_type"
    bo_boid_col: "boid"
    weight_g_col: "weight"

github_sync:
  enabled: false
  pull: true
  commit: true
  push: true
  fail_safe_on_push_conflict: true
  commit_message_template: "brickovery: {action} (run_id={run_id}, slice_id={slice_id}, upstream_sha={upstream_commit_sha})"
  git_user_name: "github-actions[bot]"
  git_user_email: "41898282+github-actions[bot]@users.noreply.github.com"

run:
  seed: 1337
  timezone: "Europe/Lisbon"
